This directory contains generated experiment outputs.

Baseline outputs are regenerated by:

```bash
python run_all.py --batch-size 8
```

The novelty modules can be rerun individually from `evaluation/`. Generated
caches, logs, large raw counterfactual rows, and raw bootstrap samples are
ignored by git; the tracked CSV/Markdown files are the compact report artifacts.
